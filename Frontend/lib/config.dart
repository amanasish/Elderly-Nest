class Config {
  // 🌟 Toggle these URLs depending on your environment 🌟
  
  // 1. Local Testing (Android Emulator)
  // static const String baseUrl = 'http://10.0.2.2:3000/api';
  
  // 2. Local Testing (iOS Simulator / Web / Real Device on same network)
  // static const String baseUrl = 'http://localhost:3000/api';
  
  // 3. Production (Render)
  static const String baseUrl = 'https://elderly-care-backend-giv2.onrender.com/api';
}
